---
const calendlyUrl = "https://calendly.com/tuusuario/intro-call"; // <-- cámbialo
const resumeUrl = "/resume/diego-ernesto-resume.pdf"; // lo creas en /public
---

<section id="cta" class="section">
  <div class="wrap">
    <div class="box">
      <div class="left">
        <h2>Let’s talk about growth, content, and conversion.</h2>
        <p>
          I build performance-first websites (Astro) and lead content, SEO, and growth strategies that drive measurable business results.
        </p>

        <div class="roles">
          <strong>Currently seeking:</strong>
          <span>Digital Marketing</span>
          <span>Growth Marketing</span>
          <span>Content Strategy</span>
          <span>Web Development roles</span>
          <span>Boston / Hybrid</span>
        </div>
      </div>

      <div class="right">
        <a
          href="https://calendly.com/diegoernestoporrasvera/30min"
          target="_blank"
          rel="noopener noreferrer"
          class="w-full rounded-2xl border border-white/20 bg-white/5 px-8 py-4 text-base font-medium text-white backdrop-blur-md transition hover:bg-white/20 flex items-center justify-center text-center"
        >
          Schedule a Call
        </a>

        <a class="btn secondary" href={resumeUrl} download>
          Download Resume (1-page)
        </a>

        <p class="mini">
          Prefer email? <a href="mailto:diegoernestoporrasvera@gmail.com">diegoernestoporrasvera@gmail.com</a>
        </p>
      </div>
    </div>
  </div>
</section>

<style>
  .section { padding: 64px 0; }
  .wrap { max-width: 1100px; margin: 0 auto; padding: 0 20px; }
  .box {
    display: grid;
    grid-template-columns: 1.5fr 0.9fr;
    gap: 18px;
    border: 1px solid rgba(255,255,255,0.12);
    border-radius: 18px;
    padding: 22px;
    background: rgba(255,255,255,0.04);
  }
  h2 { margin: 0 0 10px; font-size: clamp(22px, 3vw, 34px); }
  p { margin: 0; opacity: 0.78; max-width: 62ch; }
  .roles { margin-top: 14px; display: flex; flex-wrap: wrap; gap: 8px; align-items: center; }
  .roles strong { margin-right: 6px; }
  .roles span {
    font-size: 13px;
    padding: 6px 10px;
    border-radius: 999px;
    border: 1px solid rgba(255,255,255,0.14);
    background: rgba(255,255,255,0.03);
    opacity: 0.9;
  }

  .right { display: flex; flex-direction: column; gap: 10px; justify-content: center; }
  .btn {
    display: inline-flex;
    justify-content: center;
    align-items: center;
    padding: 12px 14px;
    border-radius: 12px;
    text-decoration: none;
    font-weight: 700;
    border: 1px solid rgba(255,255,255,0.14);
  }
.btn.primary {
  background: linear-gradient(135deg, #ffffff22, #ffffff12);
  border-color: rgba(255,255,255,0.25);
}

.btn.secondary {
  background: rgba(255,255,255,0.05);
  opacity: 0.9;
}
  .mini { margin-top: 6px; font-size: 13px; opacity: 0.75; }

  @media (max-width: 900px) {
    .box { grid-template-columns: 1fr; }
  }
</style>
