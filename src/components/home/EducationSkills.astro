---
const skills = {
  Development: ["Astro", "React", "Tailwind CSS", "HTML/CSS", "JavaScript", "WordPress"],
  Digital: ["Social Media Management", "Content Strategy", "Google Business Profile"],
  Creative: ["DaVinci Resolve", "Adobe Creative Suite", "Final Cut Pro", "Audio Production"],
};
---

<section class="mt-14 grid gap-6 md:grid-cols-2">
  <div class="rounded-2xl border border-zinc-800 bg-zinc-950 p-6">
    <h2 class="text-xl font-semibold tracking-tight">Education</h2>
    <p class="mt-3 text-sm text-zinc-300">
      <span class="font-medium text-zinc-100">B.S. Systems &amp; Informatics Engineering</span><br />
      Universidad Continental (Peru) · 2015 – 2022
    </p>
  </div>

  <div class="rounded-2xl border border-zinc-800 bg-zinc-950 p-6">
    <h2 class="text-xl font-semibold tracking-tight">Skills</h2>

    <div class="mt-4 space-y-4">
      {Object.entries(skills).map(([group, list]) => (
        <div>
          <p class="text-sm font-medium text-zinc-200">{group}</p>
          <div class="mt-2 flex flex-wrap gap-2 text-xs text-zinc-400">
            {list.map((s) => (
              <span class="rounded-full border border-zinc-800 px-3 py-1">{s}</span>
            ))}
          </div>
        </div>
      ))}
    </div>
  </div>
</section>
